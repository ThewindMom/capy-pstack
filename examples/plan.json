{
  "version": 1,
  "max_parallel": 3,
  "parent_depth": 0,
  "pstack_root": ".agents",
  "tasks": [
    {
      "id": "api",
      "repository": "owner/application",
      "role": "implementation",
      "model_role": "bug-fix",
      "goal": "Reproduce and fix duplicate export rows after retry.",
      "write": true,
      "scope_paths": ["src/export", "tests/export"],
      "base_ref": "main",
      "acceptance": ["A real export retried twice contains each row exactly once."]
    },
    {
      "id": "docs",
      "repository": "owner/application",
      "role": "implementation",
      "goal": "Document the existing retry behavior independently of the fix.",
      "write": true,
      "scope_paths": ["docs/export.md"],
      "base_ref": "main",
      "acceptance": ["The documentation matches observed behavior and passes the docs checks."]
    },
    {
      "id": "review-api",
      "repository": "owner/application",
      "role": "review",
      "goal": "Independently review the exact export fix and actual reproduction evidence.",
      "write": false,
      "scope_paths": ["src/export", "tests/export"],
      "depends_on": ["api"],
      "base_from": "api",
      "acceptance": ["The full diff and actual regression evidence have an independent verdict."]
    }
  ]
}
